name: Girando a Ampulheta — Experience Gate

on:
  push:
    branches: [main, refactor/learning-experience-v4]
    paths:
      - 'index.html'
      - 'podcast-auth.js'
      - 'podcast-auth.css'
      - 'app.js'
      - 'content-manifest.js'
      - 'psp.js'
      - 'psp-cards.json'
      - 'a11y-v4.js'
      - 'styles.css'
      - 'psp.css'
      - 'learning-v4.css'
      - 'assets-v4.css'
      - 'assets/img/**'
      - 'assets/audio/serie-1/**'
      - 'assets/audio/serie-2/**'
      - 'assets/audio/serie-3/**'
      - 'scripts/smoke_player_e2e.py'
      - 'scripts/a11y_smoke.py'
      - 'scripts/validate_learning_v4.py'
      - 'scripts/validate_auth_gate.py'
      - 'scripts/qa_n3_release.py'
      - '.github/workflows/podcast-e2e.yml'
  pull_request:
    paths:
      - 'index.html'
      - 'podcast-auth.js'
      - 'podcast-auth.css'
      - 'app.js'
      - 'content-manifest.js'
      - 'psp.js'
      - 'psp-cards.json'
      - 'a11y-v4.js'
      - 'styles.css'
      - 'psp.css'
      - 'learning-v4.css'
      - 'assets-v4.css'
      - 'assets/img/**'
      - 'assets/audio/serie-1/**'
      - 'assets/audio/serie-2/**'
      - 'assets/audio/serie-3/**'
      - 'scripts/smoke_player_e2e.py'
      - 'scripts/a11y_smoke.py'
      - 'scripts/validate_learning_v4.py'
      - 'scripts/validate_auth_gate.py'
      - 'scripts/qa_n3_release.py'
      - '.github/workflows/podcast-e2e.yml'
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  experience-gate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-python@v7
        with:
          python-version: '3.12'
      - name: Gate estrutural da experiência
        run: python scripts/validate_learning_v4.py
      - name: Gate estrutural da autenticação
        run: python scripts/validate_auth_gate.py
      - name: Instalar Playwright e axe-core
        run: |
          pip install playwright
          playwright install --with-deps chromium
          npm install --no-save axe-core
      - name: Servir site localmente
        run: |
          python -m http.server 8765 --bind 127.0.0.1 > /tmp/podcast-http.log 2>&1 &
          echo $! > /tmp/podcast-http.pid
          for i in {1..30}; do curl -fsS http://127.0.0.1:8765/ >/dev/null && exit 0; sleep 0.5; done
          cat /tmp/podcast-http.log
          exit 1
      - name: E2E autenticação, desktop, mobile, progresso e PSP
        run: python scripts/smoke_player_e2e.py http://127.0.0.1:8765/
      - name: Axe-core — gate e estados principais
        run: python scripts/a11y_smoke.py http://127.0.0.1:8765/
      - name: Smoke público após merge em main
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: |
          python - <<'PY'
          import time
          import urllib.request

          url = 'https://ricmurtapsicologia.github.io/Podcast-ATS-CBMMG/'
          marker = 'gav-learning-v4-20260902'
          last = None
          for attempt in range(1, 31):
              try:
                  with urllib.request.urlopen(url, timeout=15) as response:
                      text = response.read().decode('utf-8')
                  required = (
                      marker,
                      'content-manifest.js',
                      'learning-v4.css',
                      'podcast-auth.js',
                      'podcast-auth.css',
                      'gav-auth-pending',
                  )
                  if all(item in text for item in required):
                      print('PASS: GitHub Pages convergiu para GAV com autenticação própria.')
                      break
                  last = 'marcadores de publicação ausentes'
              except Exception as exc:
                  last = f'{type(exc).__name__}: {exc}'
              if attempt == 30:
                  raise RuntimeError(f'GitHub Pages não convergiu: {last}')
              time.sleep(5)
          PY
      - name: E2E da versão pública
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: python scripts/smoke_player_e2e.py https://ricmurtapsicologia.github.io/Podcast-ATS-CBMMG/
      - name: Axe-core da versão pública
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: python scripts/a11y_smoke.py https://ricmurtapsicologia.github.io/Podcast-ATS-CBMMG/
